# Production frontend build configuration
# Copy this file to `.env.production` and fill in your Worker URL.
# The Worker URL is the `*.workers.dev` address shown after `pnpm deploy:worker`.
VITE_WORKER_URL=https://ai-image-studio-worker-production.zackhe774.workers.dev

# Optional: Google Analytics 4 measurement ID (leave empty to disable)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX